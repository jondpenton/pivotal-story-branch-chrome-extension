{
  "name": "pivotal-story-branch-chrome-extension",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "run-s build:!\\(extension\\)",
    "build:check": "tsc",
    "build:app": "vite build",
    "build:extension": "run-s build && cp src/manifest.json dist/",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "@vitejs/plugin-react": "^1.3.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.7.1",
    "typescript": "^4.6.3",
    "vite": "^2.9.9"
  }
}
